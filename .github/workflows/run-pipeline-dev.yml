name: Run FX Pipeline

on:
  push:
    branches:
      - dev  # Trigger only when pushing to dev branch
  workflow_dispatch:  # Optional: allow manual trigger from GitHub UI

jobs:
  run-fx-pipeline:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install duckdb python-dotenv pandas requests

    - name: Run Bronze Builder
      run: python build_fx_bronze.py

    - name: Run Silver Builder
      run: python build_fx_silver.py
